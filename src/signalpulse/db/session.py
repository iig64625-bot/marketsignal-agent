"""Database session factory.""" from __future__ import annotations  from collections.abc import Iterator from contextlib import contextmanager  from sqlalchemy.orm import Session, sessionmaker  from signalpulse.db.engine import get_engine  _SessionFactory: sessionmaker | None = None   def _get_factory() -> sessionmaker:     """Return a cached :class:`sessionmaker` bound to the current engine.      The factory is built lazily so test fixtures can swap the ``database_url``     before the first session is created.     """     global _SessionFactory     if _SessionFactory is None:         _SessionFactory = sessionmaker(bind=get_engine(), expire_on_commit=False, future=True)     return _SessionFactory   def reset_session_factory() -> None:     """Clear the cached factory (test helper)."""     global _SessionFactory     _SessionFactory = None   @contextmanager def get_session() -> Iterator[Session]:     """Yield a database session.      Commits on clean exit, rolls back on exception, and always closes the     session so that connection-pool slots are released.     """     factory = _get_factory()     session = factory()     try:         yield session         session.commit()     except Exception:         session.rollback()         raise     finally:         session.close()
